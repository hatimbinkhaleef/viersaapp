import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.viersa.app',
  appName: 'viersa',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
